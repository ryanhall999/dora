import React from "react";

const Login = props => {
  console.log(props);

  return (
    <div>
      <a href="/google">
        <button>Sign in with Google</button>
      </a>
    </div>
  );
};

export default Login;
